"""Cross-pipeline shared primitives — definitions that MUST be byte-identical across
multiple builders (e.g. the geocode crosswalk join key). Pure, dependency-light
helpers only; import from here instead of copy-pasting."""
