# Indexes Are Not All You Need — Common DuckDB Pitfalls — faithful transcript

*Faithful cleanup of the ASR transcript: every word of substance preserved; only filler removed and clear mistranscriptions corrected. Not editorialized. Verbatim source: `raw/2025-09-24_indexes-are-not-all-you-need-common-duckdb-pitfalls.raw.txt`.*

**Published:** 2025-09-24.  **Source:** Conference talk (DuckCon-style).

---

All right. Yeah, hi everyone. I'm Tanya Bogajage. Happy to be here. Happy I made it just in time for the first talk. Very interesting. So I'm going to be talking about "Indexes are not all you need." And the idea of this talk is a little bit to explore ways to look into DuckDB pitfalls and, yeah, finding them and seeing what to do from there.

So maybe a little bit of background on me. I did study computer science in Germany. Moved to Amsterdam, my first time, for an internship at CWI. Met some amazing database folks there. And eventually, after my master's, came back to work at DuckDB Labs, because it was by then like a company. Yeah, that's me. And now let's jump right in.

So, DuckDB pitfalls and how to find them. I feel like that's a bit of a big promise I am having in that title. Like, am I going to give you the magic formula to find out why your queries are slow and fix it? Sadly, I won't be able to do that, but I'm at least going to try to give some starting points on looking into slow queries and what DuckDB offers that might help figuring out the cause. So by default, DuckDB does already try to choose sensible defaults, like preconfigured values. You can check them out in the documentation. So I think it's not as easy to get into a slow query, hopefully. And also, sadly, on the other hand, that means once you have a slow query, getting it to be fast can be a bit tricky, because DuckDB isn't designed to be super, super tunable like some other systems, to match exactly your use case. Maybe you will be able to, but yeah.

And to look into ways to analyze your query performance, we're going to be using indexes as a case study, which is something I've worked a lot on, and which I feel is one of these things you can either really gain something from or lose a lot of performance from. So we will be looking at some profiling, and we're going to be also looking at one function to monitor database memory.

Yep. So maybe just a little bit of a step back, because we're going to be talking about indexes, specifically tree-based indexes. So it won't be about, for example, zone maps, which like min-max statistics and stuff. So this is what a B-tree would be, like a perfect example of a tree-based index in database systems. So, historically, if we think about the traditional database systems and their very transactional workloads, we kind of maybe understand that indexes are very inherent to that, because these systems — like, think of a web shop, for example — you have like a series of point lookups, you're doing updates on a single tuple, it's all very pointy. So that is one thing where indexes, of course, are very nice, because we can very quickly jump to that place in our data and find it. The second thing is, in these traditional systems, we have like a fixed set of queries, which means we know exactly what we want to be fast, and we can then fine-tune our systems. I mean, if you think about Oracle, they're basically building their whole business model around consulting on how to get the system fast for your workload.

So that's a bit of the history here. But now the question is, what about indexes if you look at analytical workloads? And initially that was very counterintuitive, because we're no longer looking at point lookups, for example. Instead, we want to be fast with like table scans, big table scans, and we want to compute very complex aggregations. And so the focus was really this efficient scan performance, and indexes kind of fell short there. Many systems in the beginning actually said, oh, we don't need that, we don't support that. Some still do not support indexes, tree-based indexes. And also, we're now looking at — a lot in the previous presentation, we're looking at a talk, queries someone is going to be customizing, clicking together their queries — so maybe we're not even sure, what would we even be indexing here to gain any performance boost?

So then, of course, that raises the question, where do we still see and use indexes if we now just made a point of they're no longer useful? Well, so even analytical systems, they're not like this complete standalone thing. They come from — often the data comes from a different system, and maybe integrity constraints still have to hold. Maybe I do need to give some guarantees on my data when I'm analyzing it. And also, we talked about scans a lot, but we also see a lot of filters everywhere, like this — yeah, the typical "give me exactly the value 500" and then join it to something else, all the rows matching that value.

So for these cases where you want to filter your data, zone maps can help — min-max indexes, I'm using it interchangeably, bear with me. So they can help, but only if the data is pre-clustered, because then I can quickly skip over sections of the data. But if I don't have clustering, if I don't have any correlation to sorted attributes, the zone map won't help me. And then there's also another point for the filter, which is that the data size matters. So once I'm starting to scan my whole data set to find a few tuples, scans, even though they're very tuned in analytics, do become expensive. So that's kind of like why for filters maybe we're looking at a use case where an index is still useful.

Then there's the last point on this slide, which is also, yeah, important to me personally, is that I want to stress versatility here. If we want to think about DuckDB in a production or in like a pipeline use case, we cannot assume that there will only be heavy analytical queries. So maybe we need to query some metadata as part of the table, or maybe there's going to be some chores or some queries that are not analytical query happening regularly in the background. So if we want to have DuckDB be a versatile tool to use there, we might need to think about having good index support.

So, yeah, with that, let's look at indexes and DuckDB. So eventually it took some time from the very previous 2018-ish state to now, but now we have primary keys, we have foreign keys, we have unique constraints, and it's also possible to add like explicit indexes — I'm going to call them, because you need to do the CREATE INDEX SQL to use them. What does that mean? That means, and for any of these cases, in the background DuckDB adds a secondary copy of your data that it has to maintain. It has to, yeah, keep track of. Yeah, so that's what happens behind these statements.

So I'm going to now jump into the more hands-on part. I don't have a demo, it's all in slides, but they will be available, I think, so it's easy to look up the queries. So first, to start our profiling small mini journey, let's set up our table. So here I choose a very simple table. It's just a bunch of integers. It's like 10 — 100 million rows, and I'm having like a pseudo-random thing here, because we want some deterministic behavior here. So I'm generating like some values deterministically, so we know how many duplicates we expect for our profiling. We can control that with the modulo back there. And then if you look at the data, we see also it's not clustered. It's like very unclustered, which kind of helps scan not being able to cheat. So if I talk about any index now, it's always going to be this CREATE INDEX thing. It's not a unique index, it's just because there are duplicates. It's just an index we add to our single column we have on the table, which is the ID column.

So now, because we want to look at some performance numbers, one thing that we want to do is we want to enable profiling. So maybe, or probably, some of you have used this EXPLAIN ANALYZE that DuckDB offers, where you can explain and analyze your query plan. You get like a nice output, either JSON or picture-ish thing. But DuckDB also actually has a profiler, which we are actively working on expanding, which gives you a bit more metrics than the EXPLAIN ANALYZE, and maybe also makes them more usable, because we can output them for some clients already directly on the connection, so you can forward them to some observability tool, for example. So it's no longer — ideally, and that's something we're also working on — some clients have it, you can then use these JSON files and consume them somewheres and get very nice observability on your system. You can track the average latency of your queries, you can see peak buffer memory, or even per operator, if you want to pass the JSON file, you can see if you're doing, for example, a scan or an index probing operation, or what the timings of the individual operators are. So, yeah, check it out in the docs. Maya is also here today. She's also working very actively on getting cool metrics in there, like how much did you read from a file and whatnot.

So for our presentation, we will be using profiling coverage all, which means we want to profile every query, not just SELECT statements, and we want to have the output in JSON. That's, yeah, here. And the first thing we want to look into: well, how does index maintenance perform? What I mean with that is, let's bulk append 10k rows — in this case, because a row is only an integer, 10k integers — and will look at the performance if we have no indexes on the table or we have three indexes. I just created the same index three times on that column. Because every time we add data, of course, we want to also propagate it to the index. So, yeah, it really makes a big difference. So this is our query. We just again generate 10k rows. And we can see that no index takes 7 milliseconds, and if we have three indexes only, it's already at 400. Why is that? Because our indexes — they're actually not — DuckDB can bulk append easily by just writing it to the end of the table. But for indexes, for every value, we need to go onto our tree and need to find out where we need to add it. And at the moment, that's also not parallelized. So a lot of work for three indexes.

So now, maybe you're going to be saying, well, I don't care, in my workflow I have my ingestion workflow somewhere else, and once it's all ingested — happens at night — and then during the day the data is there and I only care about select performance, ingestion can take longer, it's fine. Well, in that case, there's another thing with indexes, which is index memory. Like, once I have my indexes there and they're up and running, how much memory do they use? And I'm really not making a case for indexes at the moment, but it is what it is. So we can now have a look at the index memory by using duckdb_memory, which is like a function that gives you different memory tags and shows you how much bytes these tags consume. Here I'm just saying I want the in-memory table tag, which is giving us the memory for our now created table, and the ART memory tag, which gives us the index memory. I'm also dividing it by 10 to the nine, which gives me the gigabytes — sorry, English. So, yeah, let's have a look. We can see that our index memory is actually twice the size of our table memory. Which is significant. And that is because if we have this index, we have a secondary structure. So we need to copy the entire column, its row IDs, and we have index metadata like the nodes of our tree.

And also, what I want to mention here is that we don't yet — we buffer-manage the memory, so we track it, but we don't have an eviction strategy yet. So if you have an index and even if you lazily load it, let's say at some point you might have used a lot of it, so all of that memory is active, and DuckDB is not yet evicting it. Again, hopefully we will have this fixed in some of the upcoming releases. But even, let's say you would have eviction in place, and you could get rid of the staler or least recently used index memory. If you're having a system running with a lot of concurrency and a lot of these parts of these indexes are active for your index scans, for example, that still means that these indexes create quite some additional memory pressure on top of your queries. And then, if you have a long-running query, maybe you will have to benchmark this, but maybe just this extra memory that is now used by the indexes can't be used for your query. So it could impact performance. So, yeah, even once we have eviction in, it really has to be chosen case by case if the index gives enough benefits.

So, yeah, benefits. I mean, so far not really a great thing to use. But, well, why would I then, if I have these two situations, still choose to use an index? Well, maybe you just can't not use it, because you have integrity constraints. Okay, but let's say maybe you don't need it for that. Then maybe you want to have — you have a lot of filters, and you have a lot of highly selective filters. So your filters only get you like four rows out of 10 million or 100 million. In these cases, maybe, if you know that that's a repeated query that happens in your system, you do want to add an index for this specific case. And then DuckDB also has two configurable settings here that I want to mention, is that if you know that your system gets slower once your index starts scanning too much, for example, you could decrease the maximum count. You could say, well, I know I want to use an index for this case, but I never want to scan more than five values, maybe. So now I'm a little bit in this land of actually ending up tuning your DuckDB, but I do think there are some use cases where you might end up doing that. So, yeah, you have these two values to play around with. And, with that in mind, we have seen the default value. So we're always going to try to scan a data chunk, and if we have more, we stop, because we think it's getting too expensive.

Yeah, so with that in mind, let's do a little point query here. So in our data table we created earlier, we want to scan all the values, or all the rows, that have this ID. And here I'm just counting them — like, maybe in your use case you would be joining them to a different table, and another table, we don't know. So if I have my profiling at this time — I'm using EXPLAIN ANALYZE, but you would get the same output with the profiler, I just chose EXPLAIN ANALYZE to make it a bit more readable. We can now, first of all, check: am I actually using my index scan? Because maybe you have added your index, but DuckDB chose not to. So you can first, as the first thing, have a look with EXPLAIN ANALYZE at your query plans, and you can see, in the left side, this is using a sequential scan on your table. So somehow DuckDB is not using your index scan. That, for example, could explain why, even though you thought your query would be fast, it ended up not being fast. So once you have made sure that you're using an index scan, you can see it on the right side, we have a type index scan there. You can at least be sure you're having this scan in place.

So now, next, let's actually check out how much faster your index scan is going to make this point lookup. And we can see here that, yeah, it does matter. I mean, we're still talking about milliseconds, but let's think, let's imagine the data set is even bigger. So for anything that's like a few chunks, like 4,000 values, your index — like, you're probing, your index is going to be 1 millisecond maximum. But once you're actually looking at fetching a lot of chunks, DuckDB starts doing a lot of extra work, because you're no longer scanning sequentially and directly collecting all the values. You first have to scan the index, and then you kind of have to jump around in your data to find all the matching rows for that index, because the index gives you the row IDs, and then you still have to find them in the actual data. So that's a lot of random access instead of just scanning over it once. So, yeah, with that graph here, we can see — at least if you choose to not use any indexes, maybe that's also a relevant dimension, you say, okay, I don't care, I want to scan — scanning will always give you the same performance, it's very predictable, because you know you're always going to go over the entire table, independent of how many values you're actually going to fetch, because you have to look at all of them. So, yeah. So maybe I at least managed to make a case for indexes in like some niche situations where they could be useful, and otherwise I do want to say, be careful, because they can also add a lot of overhead to your system. Oh yeah, I also wanted to highlight that we're still looking at a very small — even at 40,000 values, we're still looking at a very small percentage of our table here. So high selectivities are important.

Maybe some final thoughts on the talk. Well, what I want to stress with my talk, or what I really think is important, is that there's no rule of thumb if you use DuckDB. Like, there's no — because we try to choose kind of the best defaults anyways, there's not an obvious "you have to do this and this and this" to make your system fast, because ideally it's already fast. So if it's not, then you really have to profile and benchmark to track down which operator is slow, or maybe in a concurrent scenario, the combination of which queries is slow. And, yeah, it's really benchmarking. And then, what can also — what I've seen really useful for some bigger, like production systems, is really good observability setup. So if you have a pipeline or anything that you can like see, okay, if we have this peak traffic, then we see these query times go up. So hopefully with our profiler, we will be able to make that more and more feasible also in DuckDB, that you can really profile relevant metrics, and that you can also use this duckdb_memory query to regularly ask DuckDB, hey, how's my memory doing? What is being allocated at the moment? So, yeah, if you're interested, check out our performance guide also, and the DuckDB configuration page. Yeah, and I guess that was my talk. So, thanks and cheers.
