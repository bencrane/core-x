# Build a Local Data Lakehouse with DuckDB and DuckLake — faithful transcript

*Faithful cleanup of the ASR transcript: every word of substance preserved; only filler removed and clear mistranscriptions corrected. Not editorialized. Verbatim source: `raw/2026-07-11_build-a-local-data-lakehouse-with-duckdb-and-ducklake.raw.txt`.*

**Published:** 2026-07-11.  **Source:** "DataGuy" (YouTube review-request video)

---

Hey y'all, DataGuy here, and I'm back with yet another review request video. This time, a continuation on a local data lakehouse, this time using DuckDB, which one of my followers wanted to see. And so, what I want to prove here is that the thing about modern data lakehouses is your actual data is a handful of nice clean parquet files, but the bookkeeping files of which files went to which table, which change in which version, all of that lives in a sprawling tree of JSON and Avro and manifest files.

And DuckDB actually has a really cool solution to that, which is why we're embedding a file format to track metadata when databases have been the world's best tool for tracking metadata for 50 years. And so, that's DuckDB. So, today we're going to build a complete DuckDB data lakehouse on your laptop with time travel, schema evolution, and multi-table transactions. And then at the end, I'm going to show you that the entire metadata layer is also a database that you can run a select statement against.

So, this is again all going to be run locally. We're going to have a make demo file. And just going through this diagram: so, the whole idea is, hey, lakehouse, two things: data and the metadata that turns a bag of files into a real table you can query. And so, what we're going to do here is you'll have DuckDB keep all the data in parquet files, and then instead of a file tree, DuckDB is going to stuff all that into a SQL database, which is going to make it a lot simpler for querying.

So, this is really useful for anyone that wants lakehouse features on a local machine and wants to just test this out without standing up a catalog service or Spark cluster. And before any code, the path we're going to rock is we'll run one command, which will kick off a Python script that wipes any old lakehouse. We're always starting clean. Then we're going to run through six, seven chapters of SQL one after another just to build out our data lakehouse.

Chapter one attaches to the lakehouse, points DuckDB at a catalog file and a parquet folder. Chapter two creates two tables, customers and orders. As it does that, rows get written out as parquet. Every statement records a snapshot in the catalog. And then chapter three will show you the storage. Chapter four does time travel. Chapter five is going to involve the schema. Chapter six is going to run a transaction, change two tables at once. And chapter seven will close DuckLake entirely and open that catalog as an ordinary database and then read the metadata with our own eyes.

So first thing we need to do is actually build a self-contained environment. So we have a quick setup script here, which is just creating a virtual environment, installing the DuckDB extensions here. So importing DuckDB. So just preloading DuckDB into here. And all this is doing is pre-downloading DuckLake extension, and we're running a tiny Python snippet that's going to open DuckDB and set that extension directory to `duckdb/extensions` and then install DuckLake. So the extension binary itself lands in the project, not in the usual DuckDB in your home folder. And now the repo is going to be completely sealed.

And then our next thing to do is we have our makefile. And this is really just going to run all of our different setup and demo commands. So it's going to show you running through these different SQL files that I'm about to get into, to actually build out that data lake. So the makefile essentially is setup, demo, shell — you're going to set it up, run our demo environment, and we also have make reset for resetting the lakehouse, make clean for nuking it back to a fresh clone. And the rule also says if the Python, if my Python's Python doesn't exist, run setup, and then all of the other makefiles depend on it. So you have to run the make setup first.

And then we also just have running the setup scripts that I just showed you. So then our data that we're going to be using here, going into this data file, is just some synthetic data around customers and their orders. So just typical use case for a company. Hey, got customers and you got stuff that you're selling them. And then what we have is our generate data scripts here. And what this will do is, again, this is just generating that data here. So pretty simple. Again, just building those files that I just showed you, but just building it via Python scripts. You can run this repeatedly.

And then we have our demo.py, which is going to set up. So first we have running chapter one setup, it's going to install DuckLake, pointed at a SQL catalog and parquet. This is the conductor, so it doesn't contain a lot of SQL. It's just going to run these seven chapter scripts in order, so it's just going to run those seven SQL scripts that I just talked about. So first you have main, which is obviously the spine, and then going down into — so just going through all these files. I'll go through the SQL files in a second, but these are just going through all of them, and then also showing you a recap file. But basically main, as this is the important function here, this is just calling all those different SQL files in order to basically just, with a single connection, thread through all of them. So create, load, inspect, time travel, schema evolution, transactions, metadata reveal, and then a recap. And the order on the screen is the order of the story.

And again, all these files are literally just running SQL, connecting to Data Lake, and then running a SQL file. So going into our SQL files, what are we actually doing here? So first we're setting our extension directory to that `duckdb/extensions` that we installed it in earlier. Then we're installing DuckLake and loading DuckLake. So it's a DuckDB extension, so turning your data house into a lakehouse is just two lines here. And then down here all we're doing is the line that is this whole concept: attach this DuckLake catalog as a lake, and so make sure you read this prefix `ducklake:` followed by a path to the DuckDB files. So we're telling DuckDB, attach a lakehouse and keep its catalog, all the metadata, in this one SQL database file. Then data path `lakehouse_data` is going to be where the parquet goes, and that single statement is the entire architecture here. We have the metadata in a SQL file over here, data as a parquet over there. And that's that separation of church and state there.

So then this third option down here, use lake. So this is where we are basically saying, hey, use DuckDB, make sure you're using these — as in, making sure you're using all the extra good features around DuckDB. So then we have our second one, making sure you're using that schema. We have create and load. And so what we're doing in here is basically just create — this is just equal to create and insert some table data in these tables. So I'll breeze through it here. We're just creating our two customers and orders tables and entering all that data from those CSVs that we were running earlier. So nothing really too interesting there.

But then, inspect storage. So this is a question around data lakehouse. It's like, hey, where is my stuff physically? So this is answering that question. So first, `lake.table_info`. This is a DuckDB function that reports per table how many parquet files back it, how many bytes each parquet file takes. So it's two tables, one file each right now. And then you have the metadata, which is going to be `lake.snapshots`, which will list every version of the lakehouse so far. And this is snapshots. Zero is the catalog initializing. Then there's a snapshot for creating customers, one for creating orders, one for loading each. And every change we made is a row in a table. And the version history is in a folder of files you have to parse. It becomes a query.

And then, if I actually go back to our demo file, and we go back to lines 41 through 49 here — so after the queries, it's going to actually walk the actual lakehouse data folder and print the real parquet files with the real byte sizes. Lines 42 through 44 showed here. And then line 48, it's going to print the size, the catalog file itself. So on screen, you'll see a couple of parquet files holding the data and one DuckDB file holding all the metadata.

Then, if we go into time travel — so, or actually, first, let's go back into this lakehouse.py. So these are just different shared helpers. And I'll go into this file in more detail in a few minutes. But the file listing that we're using here within 295 here. So list data files. So list data, what makes data as parquet feel true instead of asserted. So I just want to show you that little trick there. But then going back to SQL here, so here's our time travel function. So this is a big feature why people like lakehouses, and DuckDB gets it for free here. So here we're simulating the next day's load. Count now 16 orders, revenue up to 924.3. And then the magic is select count from all these. Clause says me at version four says, hey, give me the orders table as it existed at snapshot four before today's batch. And you get back 12 and 706.919, which is the exact same number as before. And same table, same query, same point in history, and there's no restoring a backup or a separate audit copy.

And actually I can show you all these queries being run, if I pull up this table here. So here you can see the results of my make demo file. So I ran make demo here. And essentially, created load tables. Where does it live? Here it's showing you the results of those parquet files. And then down here under time travel, here we have the current state and also the revenue before, so you can see all the different states there.

So now going into schema evolution. So going into 05 here, we now are going to say, hey, schema changes in DuckDB are actually metadata only. That's the big headline feature here, in that no parquet file is ever going to get rewritten. So let me show you what that means. So if we add two columns to customers, a loyalty tier that you default to bronze, lifetime value that defaults to zero. Existing eight customers didn't have a value for this, so the default gets filled in at read time from the catalog. The old parquet files that don't have those columns are never touched. And then if we rename a column here, the name becomes full name, and again the parquet files still say name inside of them. DuckLake just records in the catalog that the column the world calls full name maps to that data. A rename just then becomes a metadata edit.

And then down here line 17, we have a select from customers, get the full name, two new columns all populated. Line 21 again same select using a different version for that changes. And then the closer here, table info, this is the file count for customers will be exactly what it was before all those alter statements. Same files, same bytes, added column, renamed a column. DuckLake actually wrote zero data because there wasn't any new data, and that's the whole promise of schema evolution. And so if we go into the schema evolution here, you'll see the file count doesn't actually change. We still just have one file for our customer data, which is exactly the point there.

So now we go on to multi-table transactions. So how can we do multi-table transactions with a local DuckLake? So here what's happening is, this is where DuckLake really shines, and is like, think about processing a real order here. You want to record the sale and update the customer's running total, and you want either both to happen or neither. So you wrap them in a begin transaction and then commit it. Insert order 1017 into orders and then bump customer three lifetime value, promote them to gold. Two tables, one atomic unit. And how do we know it was atomic? We go down here. We can look at the newest snapshot, and the changes will show both tables move together. So there's one snapshot, two tables. And an Iceberg, Delta, each table's version on its own. So cleaning atomic commit across two of them is pretty awkward. Here a multi-table transaction, coordination the format doesn't really hand you. So it's a begin and a commit, because the catalog is a real database, and real databases are able to do this really easily. So then a quick check that we'll run down here is customer three will now show gold, and we'll show the new lifetime value of 88. So the sale and customer update land together.

And then down here, on the flip side of the rollback of the atomic is also rollbacks. We want to rollback that transaction, we can open a transaction, do something dumb, and then re-tier every customer to platinum, delete every paid order, but then instead of committing, we roll it back. And that makes it basically just never happened. So we count the tiers, paid orders, they're still going to be untouched. It's forgotten away cleanly. So now, if you go into the actual example here, you can see exactly that happening. So we're inserting it. One row gets changed, updating customers, both get changed, and then here we're seeing, hey, nothing actually gets reverted when we try to run that crazy set.

And then finally down here, so we'll get into the last piece, which is, hey, how do we actually query this metadata? Why are we doing this at the end of the day? And that is because querying metadata opens up a lot of new use cases, and it's a lot easier. So because this is, and this is the whole point — so first, imagine the DuckLake connection is closed. So in the demo, we've closed this connection actually. So now we're only just going to only have the metadata connection open, so we're not actually interacting with the DuckLake DB data itself. And then we're going to do a connect in our lakehouse script online, where is it? On line 118 up here. So here what we're going to do is connect to the catalog raw, what I just said, plain DuckDB database with no DuckLake extension.

And then if we go to our SQL file now, so we want, let's say — so we want to run this against that raw connection now. So the first query is going to be listing every table whose name starts with DuckLake, and you're going to get this great list of 30 tables, and then this is that pile of metadata files that Iceberg and Delta keep on disk. And then you want your version history, you can just run a select from DuckLake snapshot, and that's the snapshot log we saw through that fancy function earlier. And then if you want to know every parquet file, you just select from DuckLake data file, and the pointers from logical tables go to physical files. So it's a table you can query.

And then finally, one of the cooler ones is that DuckLake column filter, and snapshot is null means the current live version of every column. So, full name, column rename, loyalty tier, lifetime value, all of these are going to show here, and every schema change is a plain row that we could have written ourselves. The rename isn't magic. It's just an update to the metadata table. And that's really the whole thesis of DuckLake and SQL, with the over-sharding now. So you can see all that information prints it out here just as we designed.

And that is really everything I had to show you today. I just want to show you how you can get started using DuckLake on your own machine. And really just have a great time doing it. It's a really useful tool for both demoing personal projects, and even production use cases. So I encourage you to check it out and have a great rest of your day.
